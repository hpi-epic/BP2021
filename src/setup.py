# This file is needed by pip to install the src-folder and its subfolders as a package.
from setuptools import find_packages, setup

setup(name='AlphaBusiness', version='1.0', packages=find_packages())
